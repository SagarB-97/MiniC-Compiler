#include <stdio.h>

// T5
/*
Errors include :
    - Unbalanced quotes
    - Invalid tokens
*/
void main()
{
    char x = 'g', y, z;
    scanf("%c %c",&x, &y);
    z = x + y;
    int $hello, 7new = 3;

    printf("Result = %c, z);
}